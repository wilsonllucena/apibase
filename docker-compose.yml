version: '3.7'

services:
  api:
    build: ./docker
    volumes:
      - ../:/usr/src/app
      - ../node_modules:/usr/src/app/node_modules
    ports:
      - 3000:3000
      - 9229:9229
      - 3100:8080